title: Home

sections:
  fields:
    type: fields
    fields:
      related:
        label: Events in home
        type: pages
        translate: false 
        flip: true
        query: site.find('events').children.filterBy('eventStateInTime', '==', 'ended')
 