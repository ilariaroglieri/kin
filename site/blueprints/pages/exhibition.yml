title: Exhibition

columns:
  - width: 1/2
    sections:
      info:
        type: fields
        fields:
          category:
            type: tags
            max: 1
            options:
              type: query
              query: site.index.pluck("tags", ",", true)
          starting date:
            width: 1/2
            type: date
          ending date:
            width: 1/2
            type: date
          info:
            type: textarea

  - width: 1/2
    sections:
      gallery:
        type: files
        layout: cards
