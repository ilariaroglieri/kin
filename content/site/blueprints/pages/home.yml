title: Home

sections:
  fields:
    type: fields
    fields:
      text:
        type: textarea
      related:
        label: Events to display in home
        type: pages
        query: site.find('events')
 